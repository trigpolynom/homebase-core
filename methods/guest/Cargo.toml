[package]
name = "guest"
version = "0.12.0"
edition = "2021"

[workspace]

[build-dependencies]
risc0-build = "0.12"

[dependencies]
risc0-zkvm = { version = "0.12", default-features = false, features = [ "std" ] }
homebase-core = { path = "../../core" }
